# Copia este archivo como .env para Docker Compose. No confirmes .env en Git.
JWT_SECRET=reemplazar-con-un-secreto-largo-y-aleatorio
BACKEND_PORT=3000

# Genera el par con: cd backend && npm run vapid:generate
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:security@example.com
